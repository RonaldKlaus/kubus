<%= form_for(@attribute_language) do |f| %>
  <% if @attribute_language.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attribute_language.errors.count, "error") %> prohibited this attribute_language from being saved:</h2>

      <ul>
      <% @attribute_language.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :language_id %><br>
    <%= f.number_field :language_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
