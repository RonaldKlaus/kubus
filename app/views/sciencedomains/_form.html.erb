<%= form_for(@sciencedomain) do |f| %>
  <% if @sciencedomain.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sciencedomain.errors.count, "error") %> prohibited this sciencedomain from being saved:</h2>

      <ul>
      <% @sciencedomain.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
